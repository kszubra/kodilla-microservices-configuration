server:
  port: 7779

spring:
  application:
    name: cards
  profiles:
    active: production
  jpa:
    hibernate:
      ddl-auto: update

customers:
  datasource:
    url: jdbc:postgresql://localhost:5435/cardsDb
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver
